import React, {useState} from 'react'

export default function UseStateExample2(){
  // TODO: create useState hooks for this example
  // Example idea: counter + form input + derived value
  return (
    <div className="card">
      <h4>useState — example 2</h4>
      <p>// TODO: implement state, handlers and wire them to inputs/buttons</p>
    </div>
  )
}
